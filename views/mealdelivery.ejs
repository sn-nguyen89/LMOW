	<!-- Start Banner -->
	<div class="all-page-title  page-breadcrumb-mealdelivery" >
		<div class="container text-center">
			<div class="row">
				<% page.details.forEach(function (contentBox) { %>
					<% if (contentBox.contentType == "banner") { %>
						<div class="col-lg-12" >
							<h1 class="lead"><%- contentBox.title %></h1>
							<span class="lead" style="color: white; font-size: x-large;">
								<% const textArray = contentBox.description.split("\n"); %>

								<% textArray.forEach(function (text) { %>
									<%- text %>
									<br>
								<% }) %>
								<br>	
								<%if (contentBox.hasButton == "True") { %>
								<a class="btn-lg btn-circle btn-outline-new-orange"  href=<%= contentBox.buttonLink %>><%= contentBox.buttonText %></a>
								<%}%>
							</span>
							<% if(user) { %>
							<a class="btn-edit" href=<%="/edit/" + page.page + "/" + contentBox.id %>>Edit</a>
							<%}%>
						</div>
				<% }}); %>
			</div>
		</div>
	</div>
	<!-- End Banner -->

	<!-- Start Intro -->
	<div class="about-section-box" style ="background-color: lightgrey;">
		<div class="container">
			<div class="row" style =  'padding-top: 30px;'>
				<% page.details.forEach(function (contentBox) { %>
					<% if (contentBox.contentType == "intro") { %>
						<div class="col-lg-6 col-md-12 col-sm-12">
							<img src="<%= contentBox.imagePath %>" class="img-fluid"><br /><br />
						</div>
						<div class="col-lg-6 col-md-12 col-sm-12 text-center">
							<div class="inner-column text-left">
								<h1><%- contentBox.title %></h1>
								<p style = "font-size: 20px; color: black">
									<% const textArray = contentBox.description.split("\n"); %>

									<% textArray.forEach(function (text) { %>
										<%- text %>
										<br>
									<% }) %>
									<br>	
									<%if (contentBox.hasButton == "True") { %>
									<a class="btn-lg btn-circle btn-outline-new-orange" style="color: white" href=<%= contentBox.buttonLink %>><%= contentBox.buttonText %></a>
									<%}%>
								</p>
							</div>
							<% if(user) { %>
							<a class="btn-edit" href=<%="/edit/" + page.page + "/" + contentBox.id %>>Edit</a>
							<% }%>
						</div>
				<% }}); %>
			</div>
		</div>
	</div>
	<!-- End Intro -->
	<!-- Start Content Box -->
	<div class="stuff-box">
		<div class="container">
			<div class="row">
				<% page.details.forEach(function (contentBox) { %>	
					<% if (contentBox.contentType == "contentBox") { %>
					<div class="col-md-4 col-sm-6">
						<div>	
							<div class="c--anim-btn"style="text-align: center;">

								<span class="c-anim-btn">
									<p><img src=<%= contentBox.imagePath %> style="margin-top:-5px;"> </a></p>
									<h2 style="font-size: xx-large;"><b><%- contentBox.title %></b></h2>
								</span>
								<span style="font-size: larger;">

									<% const textArray = contentBox.description.split("\n"); %>

									<% textArray.forEach(function (text) { %>
									<%- text %>
									<br>
									<% }) %>
									<br>
									<%if (contentBox.hasButton == "True") { %>									
									<a class="btn-lg btn-circle btn-outline-new-orange" href=<%= contentBox.buttonLink %>><%= contentBox.buttonText %></a>
									<% } %>
								</span>
								
							</div>
							
						</div>	
						<% if(user) { %>
						<a class="btn-edit" href=<%="/edit/" + page.page + "/" + contentBox.id %>>Edit</a>
						<%}%>
					</div>
					
					<% }}); %>

				<!-- Added the icn and button for inventory -->
				<div class="col-md-4 col-sm-6">
					<div>
						<div class="c--anim-btn"style="text-align: center;">

							<span class="c-anim-btn">
								
								<p><img src="images/snowflake.png" style="margin-top:-5px;"> </a></p>
								<h2 style="font-size: xx-large;"><b>frozen Foods Inventory</b></h2>
							</span>
							<span style="font-size: larger;">
								Checkout the list of available frozen foods
								<br><br> 
								<a class="nav-item " style='background-color:green;font-weight:bold;border-radius: 5px; padding: 4px; color:white;' href="/inventory">Inventory</a>
							</span>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
