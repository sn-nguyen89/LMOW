
	<div class="all-page-title  page-breadcrumb-mealdelivery">
		<div class="container text-center">
			<div class="row">
				<% page.details.forEach(function (contentBox) { %>
					<% if (contentBox.contentType == "banner") { %>
						<div class="col-lg-12" >
							<h1 class="lead"><%- contentBox.title %></h1>
							<span class="lead" style="color: white; font-size: x-large;">
								<% const textArray = contentBox.description.split("\n"); %>

								<% textArray.forEach(function (text) { %>
									<%- text %>
									<br>
								<% }) %>
								<br>	
								<%if (contentBox.hasButton == "True") { %>
								<a class="btn-lg btn-circle btn-outline-new-orange"  href=<%= contentBox.buttonLink %>><%= contentBox.buttonText %></a>
								<%}%>
							</span>
							<% if(user) { %>
							<a class="btn-edit" href=<%="/edit/" + page.page + "/" + contentBox.id %>>Edit</a>
							<% } %>
						</div>
				<% }}); %>
			</div>
		</div>
	</div>
	<br>
    <!-- Sign Me Up -->
	<div class="container hidden-anchor" id="signmeup"> </div>
	<div class="container">
		<div class="row text">
			<% page.details.forEach(function (contentBox) { %>
				<% if (contentBox.contentType == "intro") { %>
					<div class="col-lg-12 text-center">
						<div class="heading-title text-center">
							<% if (contentBox.imagePath != "") { %>
							<img src=<%=contentBox.imagePath%> style= "max-height:300px">
							<%}%>
							<h2><%-contentBox.title %></h2>
							<span style="font-size:large; color: black;">
							<% const textArray = contentBox.description.split("\n"); %>
	
							<% textArray.forEach(function (text) { %>
								<%- text %>
								<br>
							<% }) %>
							<br>
							<%if (contentBox.hasButton == "True") { %>
								<a class="btn-lg btn-circle btn-outline-new-orange"  href=<%= contentBox.buttonLink %>><%= contentBox.buttonText %></a>
							<%}%>
							</span>						
						</div>
						<% if (user) { %> 
						<a class="btn-edit" href=<%="/edit/" + page.page + "/" + contentBox.id %>>Edit</a>
						<% } %>
					</div>
			<%}})%>
			
			
			<div class="row" style='margin:-20px 16px'>
				<%page.details.forEach(function (contentBox){ %>
					<% if (contentBox.contentType == "textBlock") { %>

						<div class="col-lg-6" style='padding:16px'>
							<h2><strong><%- contentBox.title %></strong></h2>
							<span style = "font-size: 18px; color: black; line-height: 1;">
								<% const textArray = contentBox.description.split("\n"); %>

								<% textArray.forEach(function (text) { %>
									<%- text %>
									<br>
								<% }) %>
								<br>
							
								<%if (contentBox.hasButton == "True") { %>
									<a class="btn-lg btn-circle btn-outline-new-orange"  href=<%= contentBox.buttonLink %>><%= contentBox.buttonText %></a>
								<%}%>
							</span>
							<% if (user) { %> 
							<a class="btn-edit" href=<%="/edit/" + page.page + "/" + contentBox.id %>>Edit</a>
							<% } %>
						</div>
				<% }})%>

				<!-- <h3><strong> If you are a Veteran, you may be able to get the costs of your meals paid by Veteran Affairs.
						<br />Please call
						1-866-522-2122 to inquire about eligibility.</strong></h3> -->
			</div>
			
			<br /><br />

		</div>
		<div class="container-fluid text-center">
			<%page.details.forEach(function (contentBox){ %>
				<% if (contentBox.contentType == "outro") { %>

					<div class="col-lg-12 text-center" style='padding:16px'>
						<h2><strong><%- contentBox.title %></strong></h2>
						<span style = "font-size: 18px; color: black; line-height: 1;">
							<% const textArray = contentBox.description.split("\n"); %>

							<% textArray.forEach(function (text) { %>
								<%- text %>
								<br>
							<% }) %>
							<br>
						
							<%if (contentBox.hasButton == "True") { %>
								<a class="btn-lg btn-circle btn-outline-new-orange"  href=<%= contentBox.buttonLink %>><%= contentBox.buttonText %></a>
							<%}%>
						</span>
						<% if (user) { %> 
						<a class="btn-edit" href=<%="/edit/" + page.page + "/" + contentBox.id %>>Edit</a>
						<% } %>
					</div>
			<% }})%>
		</div>
		<br /><br />
	</div>
